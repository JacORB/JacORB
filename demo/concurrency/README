Objective:
    This is an interactive program, shows how to use transactions, locking 
    unlocking resourses, Chnaging the resorse lock status, Rollback a
    transaction and commit a transaction.
    
How to Run:
    Compile:
        ant
    Run NamingServer:
        ns.bat c:/prahalad/NS_Ref
    Run Server:
        jacotemplate.bat demo.concurrency.Server
    Run Client:
        jacotemplate.bat demo.concurrency.Client

Note:
    Make sure ns.bat is using jacotemplate.bat because there is no jaco.bat in
    this cvs version of the JacORB.

    Make sure that jacorb.properties is in place(in the current dir) and has 
    the name server reference such as -
    ORBInitRef.NameService=file:/c:/prahalad/NS_Ref