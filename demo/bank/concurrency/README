Objective:
   This demo shows how to use CosConcurrencyControl service in th real 
   application.How to prevent Dead lock and have safe concurrent invocations 
   without coruppting the data.

How to Run:
    Compile:
        ant
    Run NamingServer:
        ns.bat c:/prahalad/NS_Ref
    Run Server:
        jacotemplate.bat demo.bank.concurruncy.Server
    Run Client:
        jacotemplate.bat demo.bank.concurrency.Client
Note:
    Make sure ns.bat is using jacotemplate.bat because there is no jaco.bat in
    this cvs version of the JacORB.
	
    Make sure that jacorb.properties is in place and has the name server 
    reference such as ORBInitRef.NameService=file:/c:/prahalad/NS_Ref
