

This is almost an exact replica of bugjac788 test. The reason that the
test stubs have been checked into CVS is that they have been compiled with
an earlier version of the IDL compiler (pre portable interceptor/java mapping
change). This is to test backward compatibility.
